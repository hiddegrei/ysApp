{% extends './base.html.twig' %}



{% block body %}
	{% for post in posts %}
		<div class="sm:w-3/5 w-4/5 mx-auto pt-8  border-b border-gray-200">
			<h1 class="pt-20 sm:text-5xl md:text-7xl text-4xl font-bold text-center">
				{{ post.title }}
			</h1>


			<div>
				<p class="text-xl py-6">
					{{ post.body }}
				</p>
			</div>


			<div
				class="mb-5">{# <p class="text p-5 ">
									{% if blog.user %}
					
										by:
										<a class=" m-5  border border-gray-500 text-white-600 text-s p-2 rounded transition transition-all bg-blue-500 hover:bg-blue-800 hover:text-white" href="/profile/{{blog.user.id}}">{{blog.user.username}}</a>
					
									{% endif %}
								</p> #}


			</div>

			{# <div class="mb-8">
						{{ block("comments", "./comments/index.html.twig") }}
						{{ block("createComment", "./comments/create.html.twig") }}
					</div>
			
					{% if blog.user==app.user or is_granted('ROLE_ADMIN') %}
						<a href="/blogs/edit/{{ blog.id }}" class="uppercase  border border-gray-500 text-gray-600 text-lg py-4 px-12 rounded transition transition-all hover:bg-gray-800 hover:text-white">
							edit post
						</a>
			
						<a href="/blogs/delete/{{ blog.id }}" class="ml-6 uppercase border border-gray-500 text-gray-600 text-lg py-4 px-12 rounded transition transition-all bg-red-500 hover:bg-red-800 hover:text-white">
							delete post
						</a>
					{% endif %} #}


		</div>
	{% endfor %}
{% endblock %}
